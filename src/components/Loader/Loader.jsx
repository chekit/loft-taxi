import React from 'react';

import './Loader.scss';

export const Loader = () => {
    return (
        <div className="loader">
            <p className="loader__text">Loading...</p>
        </div>
    );
}